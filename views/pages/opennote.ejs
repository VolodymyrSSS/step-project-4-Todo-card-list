<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head %>
</head>

<body>
    <!-- <div class="modal fade" id="noteModal" tabindex="-1" role="dialog" aria-labelledby="noteModalTitle" 
  aria-hidden="true">-->
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header bg-info">
                <img src="img/noteImg.png" class="img-responsive" alt="note">
                <a href="/" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </a>
            </div>
            <div class="modal-body bg-warning">
                <section class="container h-100">
                    <form class="needs-validation d-flex flex-column justify-content-center align-items-center"
                        novalidate>
                        <div class="form-group col-sm-8 text-center">
                            <label for="themeNote" class="badge badge-info text-wrap pt-sm-1 pb-sm-1"
                                style="font-size: 1rem">
                                Title your note</label>
                            <input type="text" class="form-control" name="themeNote" id="themeNote"
                                value="<%= note.title %>" placeholder="Some theme" required>
                            <div class="invalid-feedback">
                                Please provide a valid theme.
                            </div>
                        </div>
                        <div class="form-group col-sm-10 text-center">
                            <label for="textNote" class="badge badge-info text-wrap pt-sm-1 pb-sm-1"
                                style="font-size: 1rem">
                                Your comment</label>
                            <textarea class="form-control" name="textNote" id="textNote" rows="10"
                                placeholder="Some text"><%= note.text %></textarea>
                        </div>

                    </form>

                </section>
            </div>
            <div class="modal-footer border-primary bg-info">
                <button type="button" id="noteEditBtn" class="btn btn-outline-danger btn-success"
                    data-id="<%= note._id %>">
                    Save note
                </button>
            </div>

        </div>
    </div>
    <!-- </div> -->

    <% include ../partials/footer %>



</body>

</html>